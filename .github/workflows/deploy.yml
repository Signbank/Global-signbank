name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest changes
        run: |
          source /var/www/env/bin/activate
          cd /var/www/repo
          sh deploy.sh
